<title>ADD IMAGE</title>
{% for posts in post %}

    <img style="height: 200px;width: 200px" src="{{ posts.short_content }}">

{% else %}
    no records found
{% endfor %}

<form action="{{ path('post_image_index', {'id': postid}) }}" method="post" enctype="multipart/form-data">

    Select image to upload:
    <input type="file" name="imagename" id="fileToUpload">
    <input type="submit" value="Upload Image" name="submit">
</form>